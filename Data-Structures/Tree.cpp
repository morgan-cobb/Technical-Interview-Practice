#include <iomanip>
using namespace std;

struct treeNode {
    int data;
    treeNode* left;
    treeNode* right;

    treeNode(int val){
        data = val;
        left = nullptr;
        right = nullptr;
    }
};

int main(){

    //initialize tree
    treeNode one(1);
    treeNode two(2);

    //print inorder

    //print preorder


    //print postoder
    return -1;
}